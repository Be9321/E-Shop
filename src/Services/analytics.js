export const trackEvent = (userId, eventName, eventData) => {
    // Implement your analytics tracking logic here
    console.log(`User ${userId} performed event ${eventName} with data ${JSON.stringify(eventData)}`);
   };
   